numCoords, radius = [int(x) for x in input().split()]

coords = []


# def angle



for _ in range(numCoords):
    coords.append(input().split())

x1, y1 = min(coords, key=lambda x: x[1])

# sorted(coords, key = lambda x: x[1])


    